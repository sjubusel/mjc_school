dependencies {
    implementation project(':model')
    implementation project(':repository')
    implementation group: 'org.springframework', name: 'spring-tx', version: spring_framework_version // in order to use @Transactional
}
